spring:
  application:
    name: uid-generator
  datasource:    
    driver-class-name: com.mysql.jdbc.Driver
    url: 'jdbc:mysql://127.0.0.1:3306/uidgenerator?autoReconnect=true&zeroDateTimeBehavior=convertToNull&useUnicode=true&characterEncoding=utf-8&useSSL=false'
    username: uidgenerator
    password: uidgenerator
    
---
spring:
  profiles: cache

uidgenerator:
  enable-cached: true